apiVersion: v1
kind: Service
metadata:
  name: student-survey-service
spec:
  type: NodePort
  selector:
    app: student-survey
  ports:
    - port: 8081
      targetPort: 8081
      nodePort: 31436
